namespace DSHSharp.Core.Tests;

/// <summary>
/// 骨架占位测试：后续为 DSHSharp.Core 补充真实业务逻辑后，替换为有意义的测试。
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Skeleton_Is_Ready()
    {
        Assert.True(true);
    }
}
